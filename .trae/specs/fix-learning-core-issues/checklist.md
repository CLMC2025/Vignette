# Checklist

## 核心功能验证
- [x] 标记单词为"已知"后，该单词不出现在系统词书的学习队列中
- [x] 标记单词为"已知"后，该单词不出现在用户词书的学习队列中
- [x] 标记单词为"已知"后，该单词不出现在复习队列中
- [x] 新词按词书中的原始顺序排列（而非随机顺序）

## 语境设置验证
- [x] 在设置页面修改语境风格后，新生成的故事使用正确的风格
- [x] 在设置页面修改难度级别后，新生成的故事使用正确的难度
- [x] ReadPage 加载时正确读取并应用语境设置
- [x] StoryController 正确接收并使用语境设置

## 代码质量验证
- [x] `getNewWordsQueueBySystemWordBook` 使用 `sw.created_at` 排序
- [x] `getLearningQueueByUserWordBook` 新词查询使用 `sw.created_at` 排序
- [x] 所有学习队列查询方法正确过滤 KNOWN 状态
- [x] 枚举值与存储的字符串/数字正确匹配

## 排序验证
- [x] 新词队列按词书顺序排列
- [x] 复习队列按到期时间排列
- [x] 学习队列（复习+新词）的组合顺序正确

## 边界情况验证
- [x] 当所有单词都被标记为 KNOWN 时，学习队列为空
- [x] 当没有复习单词时，学习队列只包含新词
- [x] 当没有新词时，学习队列只包含复习单词
